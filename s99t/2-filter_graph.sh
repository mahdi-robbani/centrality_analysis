# Second step in the Pyinteraph pipeline: use filter_graph to filter edges based on a persistency cutoff.

# Run filter_graph
filter_graph -d hc_graph.dat -o hc_graph_filtered.dat -t 20 -p clusters_plot.pdf


